<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_white"
    tools:context=".ui.home.info.FuelPricesActivity">

    <ImageView
        android:id="@+id/iv_back"
        android:layout_width="@dimen/nav_buttons"
        android:layout_height="@dimen/nav_buttons"
        android:layout_alignParentTop="true"
        android:layout_margin="15dp"
        android:layout_marginBottom="5dp"
        android:src="@drawable/ic_baseline_arrow_back_24"
        app:tint="@color/basic_red" />

    <TextView
        android:id="@+id/tv_heading"
        style="@style/HeadingsStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/iv_back"
        android:layout_marginStart="30dp"
        android:layout_marginTop="10dp"
        android:text="@string/fuel_prices"
        android:textStyle="normal" />

    <ExpandableListView
        android:id="@+id/elv_fuels"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tv_heading"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="30dp"
        android:divider="@color/app_white"
        android:dividerHeight="1dp"
        android:groupIndicator="@drawable/selector_expandable_list"
        android:layoutDirection="rtl"
        android:scrollbars="none" />


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="10dp"
        android:text="Via cijenegoriva.info"
        android:textColor="@color/light_grey"
        android:textSize="12sp" />
</RelativeLayout>